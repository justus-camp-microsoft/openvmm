# igvmfilegen

`igvmfilegen` is the tool that assembles IGVM files for OpenHCL from a
JSON manifest and a set of binary resources.

For inspecting or comparing IGVM files, see
[igvminspect](./igvminspect.md).
